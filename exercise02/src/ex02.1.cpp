#include "myStatFunc.h"
#include "random.h"
#include <cmath>
#include <cstdlib>
#include <fstream>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

inline double integrand(double x) { return M_PI_2 * cos(M_PI_2 * x); }

inline double non_unif_distr(double x) { return 0; }
int main(int argc, char *argv[]) { return 0; }